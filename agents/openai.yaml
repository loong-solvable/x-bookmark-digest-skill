interface:
  display_name: "X Bookmark Digest"
  short_description: "Export, classify, and summarize X or Twitter bookmarks"
  default_prompt: "Use $x-bookmark-digest on macOS to run the self-contained bookmark pipeline end to end: export a logged-in user's X bookmarks into local artifacts, then produce a categorized digest, reading queue, and bookmark index."
